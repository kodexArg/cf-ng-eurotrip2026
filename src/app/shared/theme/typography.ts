/** City name headline inside the map marker popup. */
export const MARKER_TITLE_FS = '14px';

/** Subtitle / meta line (dates, nights, count) in the marker popup. */
export const MARKER_META_FS = '10.5px';

/** Day-section header inside the marker popup hito list. */
export const MARKER_DAY_HEADER_FS = '10px';

/** Hito title text inside the marker popup. */
export const MARKER_HITO_TITLE_FS = '11.5px';

/** Time gutter and notes text inside the marker popup. */
export const MARKER_SMALL_FS = '9.5px';

/** Icon size used for hito icons in the marker popup. */
export const MARKER_ICON_FS = '12px';

/** Tiny badge/indicator font size in the marker popup. */
export const MARKER_BADGE_FS = '9px';
